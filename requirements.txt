# Core dependencies
pandas>=1.5.0
pyyaml>=6.0
jupyter>=1.0.0
scikit-learn>=1.0.0
plotly>=5.0.0
matplotlib>=3.5.0
scipy>=1.9.0

# Embedding generation
sentence-transformers>=3.0.0
einops>=0.7.0
tqdm>=4.60.0

# ONNX runtime for faster CPU inference (optional but recommended)
optimum[onnxruntime]>=1.17.0

# LLM report generation
anthropic>=0.39.0

# PDF report generation
weasyprint>=61.0
kaleido>=0.2.1

# Clustering and dimensionality reduction
umap-learn>=0.5.0
hdbscan>=0.8.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Development
ipython>=8.0.0
